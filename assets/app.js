const $=s=>document.querySelector(s),$$=s=>[...document.querySelectorAll(s)];
const KEY="atlas.workspace.v3",LEGACY_KEYS=["atlas.workspace.v2","atlas.localTrips.v1"],routes=["discover","compare","shortlist","itinerary","budget","ready"];
const copy={discover:["Discover","Start with the route and dates"],compare:["Compare","Review live search snapshots"],shortlist:["Shortlist","Keep the strongest candidates"],itinerary:["Itinerary","Build the days"],budget:["Prepare","Budget and pack"],ready:["Ready","Review and deliberately hand off"]};
const defaults=["Passport / ID","Flight check-in","Power adapter","Medication","Travel insurance"];
let state=load(),pendingProvider=null;
function blank(){return{schemaVersion:3,trip:null,results:null,shortlist:[],activities:[],expenses:[],checks:defaults.map(label=>({label,done:false})),recentSearches:[],fareCalendar:{}}}
function load(){try{let raw=localStorage.getItem(KEY),source=KEY;if(!raw){source=LEGACY_KEYS.find(key=>localStorage.getItem(key))||KEY;raw=localStorage.getItem(source)}const value={...blank(),...JSON.parse(raw||"{}"),schemaVersion:3};if(source!==KEY&&raw){localStorage.setItem(KEY,JSON.stringify(value));LEGACY_KEYS.forEach(key=>localStorage.removeItem(key))}return value}catch{return blank()}}
function save(){localStorage.setItem(KEY,JSON.stringify(state));renderAll()}
function el(tag,text,cls){const n=document.createElement(tag);if(text!==undefined)n.textContent=text;if(cls)n.className=cls;return n}
function addSearchControls(){const grid=$(".form-grid");[["Adults","adults","number",[["min","1"],["max","9"],["value","1"]] ],["Children","children","number",[["min","0"],["max","8"],["value","0"]]]].forEach(([title,name,type,attrs])=>{const label=el("label",title),input=document.createElement("input");input.name=name;input.type=type;attrs.forEach(([key,value])=>input.setAttribute(key,value));label.append(input);grid.append(label)});[["Cabin class","travel_class",[["1","Economy"],["2","Premium economy"],["3","Business"],["4","First"]]],["Stops","stops",[["0","Any number"],["1","Nonstop only"],["2","1 stop or fewer"],["3","2 stops or fewer"]]],["Currency","currency",[["INR","INR"],["USD","USD"],["EUR","EUR"],["GBP","GBP"],["AED","AED"],["SGD","SGD"],["JPY","JPY"]]]].forEach(([title,name,options])=>{const label=el("label",title),select=document.createElement("select");select.name=name;options.forEach(([value,text])=>{const option=el("option",text);option.value=value;select.append(option)});label.append(select);grid.append(label)})}
function addTripControls(){const grid=$(".form-grid"),tripLabel=el("label","Trip type"),trip=document.createElement("select");trip.name="trip_type";[["one_way","One way"],["round_trip","Round trip"]].forEach(([value,text])=>{const option=el("option",text);option.value=value;trip.append(option)});tripLabel.append(trip);const returnLabel=el("label","Return date"),returnInput=document.createElement("input");returnInput.name="return_date";returnInput.type="date";returnInput.disabled=true;returnLabel.append(returnInput);const flexLabel=el("label","Flexible dates"),flex=document.createElement("select");flex.name="flexible_days";[["0","Exact date"],["1","±1 day"],["2","±2 days"],["3","±3 days"]].forEach(([value,text])=>{const option=el("option",text);option.value=value;flex.append(option)});flexLabel.append(flex);const swap=el("button","⇄ Swap route","ghost");swap.type="button";swap.onclick=()=>{const origin=$('[name="origin"]'),destination=$('[name="destination"]'),value=origin.value;origin.value=destination.value;destination.value=value;persistFilters()};trip.onchange=()=>{returnInput.disabled=trip.value!=="round_trip";returnInput.required=trip.value==="round_trip";if(returnInput.disabled)returnInput.value="";persistFilters()};grid.prepend(tripLabel,returnLabel,flexLabel);grid.append(swap);returnInput.min=$('[name="departure_date"]').value;restoreFilters();$("#search-form").addEventListener("input",persistFilters)}
function persistFilters(){const values=Object.fromEntries(new FormData($("#search-form")).entries());delete values.confirmed;localStorage.setItem("atlas.searchFilters.v1",JSON.stringify(values))}
function restoreFilters(){try{const values=JSON.parse(localStorage.getItem("atlas.searchFilters.v1")||"{}");Object.entries(values).forEach(([name,value])=>{const field=document.querySelector('[name="'+name+'"]');if(field)field.value=value});const trip=$('[name="trip_type"]'),back=$('[name="return_date"]');back.disabled=trip.value!=="round_trip";back.required=!back.disabled}catch{}}
function setupAutocomplete(){["origin","destination"].forEach(name=>{const input=document.querySelector('[name="'+name+'"]'),list=document.createElement("datalist");list.id=name+"-airports";input.setAttribute("list",list.id);document.body.append(list);let timer;input.addEventListener("input",()=>{clearTimeout(timer);if(input.value.trim().length<2)return;timer=setTimeout(async()=>{try{const response=await fetch("/api/airports?q="+encodeURIComponent(input.value.trim())),data=await response.json();list.replaceChildren();data.airports.forEach(item=>{const option=document.createElement("option");option.value=item.code;option.label=item.label;list.append(option)})}catch{}},180)})})}
function ensureDiscoveryTools(){const search=$(".search");if(!$("#recent-searches")){const area=el("section",undefined,"recent-area");area.id="recent-searches";search.append(area)}renderRecentSearches()}
function renderRecentSearches(){const host=$("#recent-searches");if(!host)return;host.replaceChildren();const items=state.recentSearches||[];if(!items.length)return;host.append(el("strong","Recent searches"));const row=el("div",undefined,"recent-list");items.forEach(item=>{const button=el("button",item.origin+" → "+item.destination+" · "+item.date,"ghost");button.type="button";button.onclick=()=>{document.querySelector('[name="origin"]').value=item.origin;document.querySelector('[name="destination"]').value=item.destination;document.querySelector('[name="departure_date"]').value=item.date;document.querySelector('[name="trip_type"]').value=item.trip_type||"one_way";const back=document.querySelector('[name="return_date"]');back.value=item.return_date||"";back.disabled=!item.return_date;back.required=!!item.return_date;persistFilters();navigate("discover")};row.append(button)});host.append(row)}
function renderDateChips(){if(!state.results)return;const card=$("#destination .destination");if(!card)return;const wrap=el("div",undefined,"date-chips");wrap.append(el("small","Nearby dates · select to run a new live search"));(state.results.date_options||[]).forEach(date=>{const known=state.fareCalendar&&state.fareCalendar[date],button=el("button",date+(known?" · from "+state.trip.currency+" "+known.toLocaleString():""),date===state.trip.date_label?"active":"");button.type="button";button.onclick=()=>{document.querySelector('[name="departure_date"]').value=date;navigate("discover");setTimeout(()=>$("#search-form").requestSubmit(),0)};wrap.append(button)});card.append(wrap)}
function ensureCompareControls(){const view=document.querySelector('[data-view="compare"]'),groups=$("#groups");if($("#compare-controls"))return;const bar=el("div",undefined,"compare-controls card");bar.id="compare-controls";const sort=document.createElement("select");[["recommended","Recommended"],["price","Lowest price"],["duration","Shortest duration"],["stops","Fewest stops"]].forEach(([value,text])=>{const option=el("option",text);option.value=value;sort.append(option)});sort.onchange=()=>sortResults(sort.value);const filter=document.createElement("select");[["all","All options"],["nonstop","Nonstop flights"],["rated","Rated stays"]].forEach(([value,text])=>{const option=el("option",text);option.value=value;filter.append(option)});filter.onchange=()=>filterResults(filter.value);bar.append(el("strong","Compare controls"),sort,filter);view.insertBefore(bar,groups)}
function sortResults(mode){if(!state.results)return;state.results.option_groups.forEach(group=>group.options.sort((a,b)=>mode==="price"?(a.price_value??Infinity)-(b.price_value??Infinity):mode==="duration"?(a.duration_minutes??Infinity)-(b.duration_minutes??Infinity):mode==="stops"?(a.stops??Infinity)-(b.stops??Infinity):a.id.localeCompare(b.id)));renderGroups();filterResults($("#compare-controls select:last-child").value)}
function filterResults(mode){$$(".option").forEach(card=>{card.hidden=mode==="nonstop"&&card.dataset.kind==="flight"&&card.dataset.stops!=="0"||mode==="rated"&&card.dataset.kind==="hotel"&&Number(card.dataset.rating||0)<4})}
function setLoading(active){$("#search-button").disabled=active;$("#search-button").textContent=active?"Searching live options…":"Search flights & stays";document.body.classList.toggle("is-loading",active);if(active){const groups=$("#groups");groups.replaceChildren(...Array.from({length:4},()=>el("div",undefined,"skeleton")))}}
function toast(message){const n=$("#toast");n.textContent=message;n.classList.add("show");setTimeout(()=>n.classList.remove("show"),2200)}
function money(value){return "USD "+Number(value||0).toLocaleString("en-US",{maximumFractionDigits:0})}
function route(){const wanted=location.hash.slice(1);return routes.includes(wanted)?wanted:"discover"}
function navigate(name){if(location.hash!="#"+name)location.hash=name;else renderRoute()}
function renderRoute(){const name=route(),index=routes.indexOf(name);$$(".view").forEach(v=>v.classList.toggle("active",v.dataset.view===name));$$(".flow a").forEach((a,i)=>{a.classList.toggle("active",a.dataset.route===name);a.classList.toggle("done",i<index);a.toggleAttribute("aria-current",a.dataset.route===name)});$("#step-title").textContent=copy[name][0];$("#step-hint").textContent=copy[name][1];$("#step-number").textContent=index+1;window.scrollTo({top:0,behavior:matchMedia("(prefers-reduced-motion: reduce)").matches?"auto":"smooth"})}
function image(url,alt){if(!url||!/^https:\/\//i.test(url))return el("span","✦");const img=document.createElement("img");img.src=url;img.alt=alt;img.loading="lazy";img.referrerPolicy="no-referrer";img.onerror=()=>img.replaceWith(el("span","✦"));return img}
function initTrip(data){const recent=state.recentSearches||[],calendar=state.fareCalendar||{},existing=state.trip&&state.trip.origin===data.trip.origin&&state.trip.destination===data.trip.destination;state=blank();state.recentSearches=recent;state.fareCalendar=calendar;state.trip=data.trip;state.results=data;state.activities=existing&&state.activities.length===data.trip.duration_days?state.activities:Array.from({length:data.trip.duration_days},()=>[]);const prices=data.option_groups[0].options.map(x=>Number(x.price_value)).filter(Number.isFinite);if(prices.length)state.fareCalendar[data.trip.date_label]=Math.min(...prices);const summary={origin:data.trip.origin,destination:data.trip.destination,date:data.trip.date_label,trip_type:data.trip.trip_type,return_date:data.trip.return_date};state.recentSearches=[summary,...recent.filter(x=>JSON.stringify(x)!==JSON.stringify(summary))].slice(0,6);save()}
function renderDestination(){const host=$("#destination");host.replaceChildren();if(!state.results){host.append(el("div","Run a search to see destination details.","empty card"));return}const d=state.results.destination,t=state.trip;const card=el("article",undefined,"card destination");card.dataset.accent=d.accent;const intro=el("div");intro.append(el("span","Destination brief","eyebrow"),el("h2",t.destination),el("p",d.tip));const facts=el("div",undefined,"destination-facts");facts.append(fact("Local time",d.timezone),fact("Currency",d.currency));card.append(intro,facts);host.append(card)}
function fact(label,value){const n=el("div");n.append(el("small",label),el("strong",value));return n}
function renderGroups(){const host=$("#groups");host.replaceChildren();if(!state.results){host.append(el("div","Search first to compare live options.","empty card"));return}state.results.option_groups.forEach(group=>{const section=el("section",undefined,"card panel");const head=el("div",undefined,"group-head"),left=el("div");left.append(el("h2",group.title),el("p",group.subtitle));head.append(left,el("small",group.options.length+" options"));const options=el("div",undefined,"options");(group.options.length?group.options:[{id:group.id+"-empty",kind:group.id==="flights"?"flight":"hotel",label:"No options found",detail:"Try another airport or date.",price:"—"}]).forEach(option=>options.append(optionCard(option)));section.append(head,options);host.append(section)});host.append(el("div",state.results.notice,"notice"))}
function optionCard(option){const selected=state.shortlist.some(x=>x.id===option.id&&x.kind===option.kind),card=el("article",undefined,"option"+(selected?" selected":""));card.dataset.kind=option.kind||"";card.dataset.stops=option.stops??"";card.dataset.rating=option.rating??"";const thumb=el("div",undefined,"thumb");thumb.append(image(option.image,option.label));const info=el("div");info.append(el("h3",option.label),el("p",option.detail),el("small",option.duration||""));if(option.kind==="flight"&&option.layovers&&option.layovers.length){const route=el("div",undefined,"route-detail");route.textContent="Via "+option.layovers.map(x=>x.airport+(x.duration_minutes?" · "+x.duration_minutes+" min":"")).join(", ");info.append(route)}if(option.emissions_grams)info.append(el("small",Math.round(option.emissions_grams/1000)+" kg estimated CO₂","emissions"));const side=el("div",undefined,"option-side");side.append(el("strong",option.price||"Price unavailable"));const button=el("button",selected?"Remove":"Shortlist",selected?"danger":"secondary");button.disabled=option.id.endsWith("-empty");button.onclick=()=>toggleShortlist(option);side.append(button);card.append(thumb,info,side);return card}
function toggleShortlist(option){const i=state.shortlist.findIndex(x=>x.id===option.id&&x.kind===option.kind);if(i>=0)state.shortlist.splice(i,1);else state.shortlist.push(option);save();toast(i>=0?"Removed from shortlist":"Added to shortlist")}
function renderShortlist(){const host=$("#shortlist");host.replaceChildren();if(!state.shortlist.length){host.append(el("div","No options shortlisted yet.","empty"));return}state.shortlist.forEach((item,index)=>{const row=el("div",undefined,"short-item"),info=el("div");info.append(el("strong",item.label),el("small",(item.kind==="flight"?"Flight · ":"Stay · ")+(item.price||"")));const remove=el("button","Remove","danger");remove.onclick=()=>{state.shortlist.splice(index,1);save()};row.append(info,remove);host.append(row)})}
function renderDays(){const host=$("#days");host.replaceChildren();if(!state.trip){host.append(el("div","Search for a trip before building an itinerary.","empty"));return}state.activities.forEach((items,dayIndex)=>{const day=el("section",undefined,"day"),head=el("div",undefined,"day-head");head.append(el("strong","Day "+(dayIndex+1)),el("span",items.length+" activities"));const list=el("ul",undefined,"activities");if(!items.length)list.append(el("li","Nothing planned yet."));items.forEach((item,itemIndex)=>{const row=el("li"),remove=el("button","Remove","danger");remove.onclick=()=>{state.activities[dayIndex].splice(itemIndex,1);save()};row.append(el("span",item),remove);list.append(row)});day.append(head,list);host.append(day)});$("#activity-day").max=state.activities.length||1}
function renderBudget(){const total=state.expenses.reduce((s,x)=>s+x.amount,0);$("#budget-total").textContent=money(total);const host=$("#expenses");host.replaceChildren();state.expenses.forEach((item,index)=>{const row=el("div",undefined,"expense"),remove=el("button","Remove","danger");remove.onclick=()=>{state.expenses.splice(index,1);save()};row.append(el("span",item.category),el("strong",money(item.amount)),remove);host.append(row)});const checks=$("#checks");checks.replaceChildren();state.checks.forEach((item,index)=>{const label=el("label",undefined,"consent"),input=document.createElement("input");input.type="checkbox";input.checked=item.done;input.onchange=()=>{state.checks[index].done=input.checked;save()};label.append(input,el("span",item.label));checks.append(label)})}
function readiness(){const hasFlight=state.shortlist.some(x=>x.kind==="flight"),hasStay=state.shortlist.some(x=>x.kind==="hotel"),hasPlan=state.activities.some(x=>x.length),hasBudget=state.expenses.length>0,packed=state.checks.filter(x=>x.done).length/state.checks.length;return Math.round((Number(!!state.trip)+Number(hasFlight)+Number(hasStay)+Number(hasPlan)+Number(hasBudget)+packed)/6*100)}
function renderReadiness(){const value=readiness();$("#score").style.setProperty("--score",value+"%");$("#score-value").textContent=value+"%";$("#readiness-bar").style.width=value+"%";$("#readiness-title").textContent=value>=85?"Nearly ready to go":value>=50?"Your trip is taking shape":"Build the essentials";$("#readiness-copy").textContent="Readiness considers your search, shortlisted flight and stay, itinerary, budget and packing progress."}
function renderAll(){renderDestination();renderDateChips();renderGroups();renderShortlist();renderDays();renderBudget();renderReadiness();ensureDiscoveryTools();renderRecentSearches()}
$("#search-form").addEventListener("submit",async e=>{e.preventDefault();const error=$("#error"),fd=new FormData(e.currentTarget),tripType=fd.get("trip_type"),returnDate=fd.get("return_date");setLoading(true);error.style.display="none";try{const response=await fetch("/api/search",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({origin:fd.get("origin"),destination:fd.get("destination"),departure_date:fd.get("departure_date"),duration_days:Number(fd.get("duration_days")),adults:Number(fd.get("adults")),children:Number(fd.get("children")),travel_class:Number(fd.get("travel_class")),stops:Number(fd.get("stops")),currency:fd.get("currency"),trip_type:tripType,return_date:tripType==="round_trip"?returnDate:null,flexible_days:Number(fd.get("flexible_days"))})});const data=await response.json();if(!response.ok)throw new Error(data.detail||"Search is unavailable.");initTrip(data);navigate("compare");toast("Your trip workspace is ready")}catch(err){error.textContent=(err.message||"Search is unavailable.")+" Try a nearby airport, another date, or fewer filters.";error.style.display="block"}finally{setLoading(false)}});
$("#activity-form").addEventListener("submit",e=>{e.preventDefault();if(!state.trip)return toast("Search for a trip first");const day=Number($("#activity-day").value)-1,name=$("#activity-name").value.trim();if(day<0||day>=state.activities.length||!name)return;state.activities[day].push(name);$("#activity-name").value="";save();toast("Activity added")});
$("#expense-form").addEventListener("submit",e=>{e.preventDefault();const amount=Number($("#expense-amount").value);if(!Number.isFinite(amount)||amount<0)return;state.expenses.push({category:$("#expense-category").value,amount});$("#expense-amount").value="";save();toast("Budget updated")});
$$("[data-handoff]").forEach(button=>button.onclick=()=>{if(!state.trip)return toast("Search for a trip first");pendingProvider=button.dataset.handoff;$("#handoff-copy").textContent="Continue to "+(pendingProvider==="google_flights"?"Google Flights":"Google Hotels")+" for "+state.trip.destination+"? Prices and availability must be checked again.";$("#handoff-dialog").showModal()});
$("#handoff-cancel").onclick=()=>$("#handoff-dialog").close();
$("#handoff-confirm").onclick=async()=>{const response=await fetch("/api/handoff",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({provider:pendingProvider,origin:state.trip.origin,destination:state.trip.destination,departure_date:state.trip.date_label,confirmed:true})});const data=await response.json();if(response.ok&&/^https:\/\/www\.google\.com\/travel\//.test(data.url)){window.open(data.url,"_blank","noopener,noreferrer");$("#handoff-dialog").close()}else toast("Provider handoff is unavailable")};
addSearchControls();addTripControls();setupAutocomplete();ensureCompareControls();window.addEventListener("hashchange",renderRoute);const departureField=document.querySelector('input[name="departure_date"]'),returnField=document.querySelector('input[name="return_date"]');departureField.min=new Date().toISOString().slice(0,10);if(!departureField.value)departureField.value=new Date(Date.now()+30*864e5).toISOString().slice(0,10);returnField.min=departureField.value;departureField.addEventListener("change",()=>{returnField.min=departureField.value;if(returnField.value&&returnField.value<=departureField.value)returnField.value=""});renderAll();renderRoute();
